// This is what one item requires

// One more Way of Making this ACCESIBLE Throughout the APP i.e it is same as the recipe.model.ts
export class Ingredient {
    constructor( public name: string, public amount: number ) {}
}